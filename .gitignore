# ── Secrets ──────────────────────────────────────
.env
*.env.local
*.env.*.local

# ── Python ───────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
_pydeps/
.pytest_cache/

# ── Model weights & data (too large for git) ────
*.pt
*.pth
*.onnx
*.npz
*.zip
*.tar.gz
*.h5
data/real/
data/cache/
data/synthetic/*.csv
checkpoints/trained/
checkpoints/smoke/
artifacts/

# Bundled public demo checkpoints. These are small physics/rule-distilled
# surrogates used by the deployed ML path; keep larger training outputs ignored.
!checkpoints/trained/
!checkpoints/trained/*.pt
!checkpoints/trained/checkpoint_manifest.json
!checkpoints/physics_distilled_surrogates_v1.zip

# ── Kaggle training notebooks (not needed for deploy)
kaggle_deploy/
kaggle_deploy_2/
kaggle_deploy_3/

# ── Training & research (not needed for deploy) ─
training/
validation/
reports/
business/
dashboard/
docs/

# ── OS junk ──────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── IDE ──────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
